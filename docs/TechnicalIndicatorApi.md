# TechnicalIndicatorApi

All URIs are relative to *https://api.twelvedata.com*

|Method | HTTP request | Description|
|------------- | ------------- | -------------|
|[**getTimeSeriesAd**](#gettimeseriesad) | **GET** /ad | Accumulation/distribution|
|[**getTimeSeriesAdOsc**](#gettimeseriesadosc) | **GET** /adosc | Accumulation/distribution oscillator|
|[**getTimeSeriesAdd**](#gettimeseriesadd) | **GET** /add | Addition|
|[**getTimeSeriesAdx**](#gettimeseriesadx) | **GET** /adx | Average directional index|
|[**getTimeSeriesAdxr**](#gettimeseriesadxr) | **GET** /adxr | Average directional movement index rating|
|[**getTimeSeriesApo**](#gettimeseriesapo) | **GET** /apo | Absolute price oscillator|
|[**getTimeSeriesAroon**](#gettimeseriesaroon) | **GET** /aroon | Aroon indicator|
|[**getTimeSeriesAroonOsc**](#gettimeseriesaroonosc) | **GET** /aroonosc | Aroon oscillator|
|[**getTimeSeriesAtr**](#gettimeseriesatr) | **GET** /atr | Average true range|
|[**getTimeSeriesAvg**](#gettimeseriesavg) | **GET** /avg | Average|
|[**getTimeSeriesAvgPrice**](#gettimeseriesavgprice) | **GET** /avgprice | Average price|
|[**getTimeSeriesBBands**](#gettimeseriesbbands) | **GET** /bbands | Bollinger bands|
|[**getTimeSeriesBeta**](#gettimeseriesbeta) | **GET** /beta | Beta indicator|
|[**getTimeSeriesBop**](#gettimeseriesbop) | **GET** /bop | Balance of power|
|[**getTimeSeriesCci**](#gettimeseriescci) | **GET** /cci | Commodity channel index|
|[**getTimeSeriesCeil**](#gettimeseriesceil) | **GET** /ceil | Ceiling|
|[**getTimeSeriesCmo**](#gettimeseriescmo) | **GET** /cmo | Chande momentum oscillator|
|[**getTimeSeriesCoppock**](#gettimeseriescoppock) | **GET** /coppock | Coppock curve|
|[**getTimeSeriesCorrel**](#gettimeseriescorrel) | **GET** /correl | Correlation|
|[**getTimeSeriesCrsi**](#gettimeseriescrsi) | **GET** /crsi | Connors relative strength index|
|[**getTimeSeriesDema**](#gettimeseriesdema) | **GET** /dema | Double exponential moving average|
|[**getTimeSeriesDiv**](#gettimeseriesdiv) | **GET** /div | Division|
|[**getTimeSeriesDpo**](#gettimeseriesdpo) | **GET** /dpo | Detrended price oscillator|
|[**getTimeSeriesDx**](#gettimeseriesdx) | **GET** /dx | Directional movement index|
|[**getTimeSeriesEma**](#gettimeseriesema) | **GET** /ema | Exponential moving average|
|[**getTimeSeriesExp**](#gettimeseriesexp) | **GET** /exp | Exponential|
|[**getTimeSeriesFloor**](#gettimeseriesfloor) | **GET** /floor | Floor|
|[**getTimeSeriesHeikinashiCandles**](#gettimeseriesheikinashicandles) | **GET** /heikinashicandles | Heikinashi candles|
|[**getTimeSeriesHlc3**](#gettimeserieshlc3) | **GET** /hlc3 | High, low, close average|
|[**getTimeSeriesHtDcPeriod**](#gettimeserieshtdcperiod) | **GET** /ht_dcperiod | Hilbert transform dominant cycle period|
|[**getTimeSeriesHtDcPhase**](#gettimeserieshtdcphase) | **GET** /ht_dcphase | Hilbert transform dominant cycle phase|
|[**getTimeSeriesHtPhasor**](#gettimeserieshtphasor) | **GET** /ht_phasor | Hilbert transform phasor components|
|[**getTimeSeriesHtSine**](#gettimeserieshtsine) | **GET** /ht_sine | Hilbert transform sine wave|
|[**getTimeSeriesHtTrendMode**](#gettimeserieshttrendmode) | **GET** /ht_trendmode | Hilbert transform trend vs cycle mode|
|[**getTimeSeriesHtTrendline**](#gettimeserieshttrendline) | **GET** /ht_trendline | Hilbert transform instantaneous trendline|
|[**getTimeSeriesIchimoku**](#gettimeseriesichimoku) | **GET** /ichimoku | Ichimoku cloud|
|[**getTimeSeriesKama**](#gettimeserieskama) | **GET** /kama | Kaufman adaptive moving average|
|[**getTimeSeriesKeltner**](#gettimeserieskeltner) | **GET** /keltner | Keltner channel|
|[**getTimeSeriesKst**](#gettimeserieskst) | **GET** /kst | Know sure thing|
|[**getTimeSeriesLinearReg**](#gettimeserieslinearreg) | **GET** /linearreg | Linear regression|
|[**getTimeSeriesLinearRegAngle**](#gettimeserieslinearregangle) | **GET** /linearregangle | Linear regression angle|
|[**getTimeSeriesLinearRegIntercept**](#gettimeserieslinearregintercept) | **GET** /linearregintercept | Linear regression intercept|
|[**getTimeSeriesLinearRegSlope**](#gettimeserieslinearregslope) | **GET** /linearregslope | Linear regression slope|
|[**getTimeSeriesLn**](#gettimeseriesln) | **GET** /ln | Natural logarithm|
|[**getTimeSeriesLog10**](#gettimeserieslog10) | **GET** /log10 | Base-10 logarithm|
|[**getTimeSeriesMa**](#gettimeseriesma) | **GET** /ma | Moving average|
|[**getTimeSeriesMacd**](#gettimeseriesmacd) | **GET** /macd | Moving average convergence divergence|
|[**getTimeSeriesMacdExt**](#gettimeseriesmacdext) | **GET** /macdext | Moving average convergence divergence extension|
|[**getTimeSeriesMacdSlope**](#gettimeseriesmacdslope) | **GET** /macd_slope | Moving average convergence divergence slope|
|[**getTimeSeriesMama**](#gettimeseriesmama) | **GET** /mama | MESA adaptive moving average|
|[**getTimeSeriesMax**](#gettimeseriesmax) | **GET** /max | Maximum|
|[**getTimeSeriesMaxIndex**](#gettimeseriesmaxindex) | **GET** /maxindex | Maximum Index|
|[**getTimeSeriesMcGinleyDynamic**](#gettimeseriesmcginleydynamic) | **GET** /mcginley_dynamic | McGinley dynamic indicator|
|[**getTimeSeriesMedPrice**](#gettimeseriesmedprice) | **GET** /medprice | Median price|
|[**getTimeSeriesMfi**](#gettimeseriesmfi) | **GET** /mfi | Money flow index|
|[**getTimeSeriesMidPoint**](#gettimeseriesmidpoint) | **GET** /midpoint | Midpoint|
|[**getTimeSeriesMidPrice**](#gettimeseriesmidprice) | **GET** /midprice | Midprice|
|[**getTimeSeriesMin**](#gettimeseriesmin) | **GET** /min | Minimum|
|[**getTimeSeriesMinIndex**](#gettimeseriesminindex) | **GET** /minindex | Minimum index|
|[**getTimeSeriesMinMax**](#gettimeseriesminmax) | **GET** /minmax | Minimum and maximum|
|[**getTimeSeriesMinMaxIndex**](#gettimeseriesminmaxindex) | **GET** /minmaxindex | Minimum and maximum index|
|[**getTimeSeriesMinusDI**](#gettimeseriesminusdi) | **GET** /minus_di | Minus directional indicator|
|[**getTimeSeriesMinusDM**](#gettimeseriesminusdm) | **GET** /minus_dm | Minus directional movement|
|[**getTimeSeriesMom**](#gettimeseriesmom) | **GET** /mom | Momentum|
|[**getTimeSeriesMult**](#gettimeseriesmult) | **GET** /mult | Multiplication|
|[**getTimeSeriesNatr**](#gettimeseriesnatr) | **GET** /natr | Normalized average true range|
|[**getTimeSeriesObv**](#gettimeseriesobv) | **GET** /obv | On balance volume|
|[**getTimeSeriesPercentB**](#gettimeseriespercentb) | **GET** /percent_b | Percent B|
|[**getTimeSeriesPivotPointsHL**](#gettimeseriespivotpointshl) | **GET** /pivot_points_hl | Pivot points high low|
|[**getTimeSeriesPlusDI**](#gettimeseriesplusdi) | **GET** /plus_di | Plus directional indicator|
|[**getTimeSeriesPlusDM**](#gettimeseriesplusdm) | **GET** /plus_dm | Plus directional movement|
|[**getTimeSeriesPpo**](#gettimeseriesppo) | **GET** /ppo | Percentage price oscillator|
|[**getTimeSeriesRoc**](#gettimeseriesroc) | **GET** /roc | Rate of change|
|[**getTimeSeriesRocp**](#gettimeseriesrocp) | **GET** /rocp | Rate of change percentage|
|[**getTimeSeriesRocr**](#gettimeseriesrocr) | **GET** /rocr | Rate of change ratio|
|[**getTimeSeriesRocr100**](#gettimeseriesrocr100) | **GET** /rocr100 | Rate of change ratio 100|
|[**getTimeSeriesRsi**](#gettimeseriesrsi) | **GET** /rsi | Relative strength index|
|[**getTimeSeriesRvol**](#gettimeseriesrvol) | **GET** /rvol | Relative volume|
|[**getTimeSeriesSar**](#gettimeseriessar) | **GET** /sar | Parabolic stop and reverse|
|[**getTimeSeriesSarExt**](#gettimeseriessarext) | **GET** /sarext | Parabolic stop and reverse extended|
|[**getTimeSeriesSma**](#gettimeseriessma) | **GET** /sma | Simple moving average|
|[**getTimeSeriesSqrt**](#gettimeseriessqrt) | **GET** /sqrt | Square root|
|[**getTimeSeriesStdDev**](#gettimeseriesstddev) | **GET** /stddev | Standard deviation|
|[**getTimeSeriesStoch**](#gettimeseriesstoch) | **GET** /stoch | Stochastic oscillator|
|[**getTimeSeriesStochF**](#gettimeseriesstochf) | **GET** /stochf | Stochastic fast|
|[**getTimeSeriesStochRsi**](#gettimeseriesstochrsi) | **GET** /stochrsi | Stochastic relative strength index|
|[**getTimeSeriesSub**](#gettimeseriessub) | **GET** /sub | Subtraction|
|[**getTimeSeriesSum**](#gettimeseriessum) | **GET** /sum | Summation|
|[**getTimeSeriesSuperTrend**](#gettimeseriessupertrend) | **GET** /supertrend | Supertrend|
|[**getTimeSeriesSuperTrendHeikinAshiCandles**](#gettimeseriessupertrendheikinashicandles) | **GET** /supertrend_heikinashicandles | Supertrend Heikin Ashi candles|
|[**getTimeSeriesT3ma**](#gettimeseriest3ma) | **GET** /t3ma | Triple exponential moving average|
|[**getTimeSeriesTRange**](#gettimeseriestrange) | **GET** /trange | True range|
|[**getTimeSeriesTema**](#gettimeseriestema) | **GET** /tema | Triple exponential moving average|
|[**getTimeSeriesTrima**](#gettimeseriestrima) | **GET** /trima | Triangular moving average|
|[**getTimeSeriesTsf**](#gettimeseriestsf) | **GET** /tsf | Time series forecast|
|[**getTimeSeriesTypPrice**](#gettimeseriestypprice) | **GET** /typprice | Typical price|
|[**getTimeSeriesUltOsc**](#gettimeseriesultosc) | **GET** /ultosc | Ultimate oscillator endpoint|
|[**getTimeSeriesVar**](#gettimeseriesvar) | **GET** /var | Variance|
|[**getTimeSeriesVwap**](#gettimeseriesvwap) | **GET** /vwap | Volume weighted average price|
|[**getTimeSeriesWclPrice**](#gettimeserieswclprice) | **GET** /wclprice | Weighted close price|
|[**getTimeSeriesWillR**](#gettimeserieswillr) | **GET** /willr | Williams %R|
|[**getTimeSeriesWma**](#gettimeserieswma) | **GET** /wma | Weighted moving average|

# **getTimeSeriesAd**
> GetTimeSeriesAd200Response getTimeSeriesAd()

The Accumulation/Distribution (AD) endpoint provides data on the cumulative money flow into and out of a financial instrument, using its closing price, price range, and trading volume. This endpoint returns the AD line, which helps users identify potential buying or selling pressure and assess the strength of price movements.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAd(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAd200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesAdOsc**
> GetTimeSeriesAdOsc200Response getTimeSeriesAdOsc()

The Accumulation/Distribution Oscillator endpoint (ADOSC) calculates a momentum indicator that highlights shifts in buying or selling pressure by analyzing price and volume data over different time frames. It returns numerical values that help users identify potential trend reversals in financial markets.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let fastPeriod: number; //Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
let slowPeriod: number; //Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAdOsc(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    fastPeriod,
    slowPeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **fastPeriod** | [**number**] | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 12|
| **slowPeriod** | [**number**] | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 26|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAdOsc200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesAdd**
> GetTimeSeriesAdd200Response getTimeSeriesAdd()

The Addition (ADD) endpoint calculates the sum of two input data series, such as technical indicators or price data, and returns the combined result. This endpoint is useful for users who need to aggregate data points to create custom indicators or analyze the combined effect of multiple data series in financial analysis.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType1: SeriesTypeEnum; //Price type used as the first part of technical indicator (optional) (default to undefined)
let seriesType2: SeriesTypeEnum; //Price type used as the second part of technical indicator (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAdd(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType1,
    seriesType2,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType1** | **SeriesTypeEnum** | Price type used as the first part of technical indicator | (optional) defaults to undefined|
| **seriesType2** | **SeriesTypeEnum** | Price type used as the second part of technical indicator | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAdd200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesAdx**
> GetTimeSeriesAdx200Response getTimeSeriesAdx()

The Average Directional Index (ADX) endpoint provides data on the strength of a market trend, regardless of its direction. It returns a numerical value that helps users identify whether a market is trending or moving sideways.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAdx(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAdx200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesAdxr**
> GetTimeSeriesAdxr200Response getTimeSeriesAdxr()

The Average Directional Movement Index Rating (ADXR) endpoint provides a smoothed measure of trend strength for a specified financial instrument. It returns the ADXR values, which help users assess the consistency of a trend over a given period by reducing short-term fluctuations. This endpoint is useful for traders and analysts who need to evaluate the stability of market trends for better timing of entry and exit points in their trading strategies.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAdxr(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAdxr200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesApo**
> GetTimeSeriesApo200Response getTimeSeriesApo()

The Absolute Price Oscillator (APO) endpoint calculates the difference between two specified moving averages of a financial instrument\'s price, providing data that helps users identify potential price trends and reversals. The response includes the calculated APO values over a specified time period, which can be used to track momentum changes and assess the strength of price movements.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let fastPeriod: number; //Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
let slowPeriod: number; //Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
let maType: MaTypeEnum; //The type of moving average used (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesApo(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    fastPeriod,
    slowPeriod,
    maType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **fastPeriod** | [**number**] | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 12|
| **slowPeriod** | [**number**] | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 26|
| **maType** | **MaTypeEnum** | The type of moving average used | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesApo200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesAroon**
> GetTimeSeriesAroon200Response getTimeSeriesAroon()

The Aroon Indicator endpoint provides data on the time elapsed since the highest high and lowest low within a specified period, helping users identify the presence and strength of market trends. It returns two values: Aroon Up and Aroon Down, which indicate the trend direction and momentum. This endpoint is useful for traders and analysts looking to assess trend patterns and potential reversals in financial markets.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAroon(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAroon200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesAroonOsc**
> GetTimeSeriesAroonOsc200Response getTimeSeriesAroonOsc()

The Aroon Oscillator endpoint provides the calculated difference between the Aroon Up and Aroon Down indicators for a given financial instrument. It returns a time series of values that help users identify the strength and direction of a trend, as well as potential trend reversals. This data is useful for traders and analysts seeking to evaluate market trends over a specified period.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAroonOsc(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAroonOsc200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesAtr**
> GetTimeSeriesAtr200Response getTimeSeriesAtr()

The Average True Range (ATR) endpoint provides data on market volatility by calculating the average range of price movement over a user-defined period. It returns numerical values representing the ATR for each time interval, allowing users to gauge the degree of price fluctuation in a financial instrument. This data is useful for setting stop-loss levels and determining optimal entry and exit points in trading strategies.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAtr(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAtr200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesAvg**
> GetTimeSeriesAvg200Response getTimeSeriesAvg()

The Average (AVG) endpoint calculates the arithmetic mean of a specified data series over a chosen time period. It returns a smoothed dataset that helps users identify trends by reducing short-term fluctuations. This endpoint is useful for obtaining a clearer view of data trends, particularly in time series analysis.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAvg(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAvg200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesAvgPrice**
> GetTimeSeriesAvgPrice200Response getTimeSeriesAvgPrice()

The Average Price (AVGPRICE) endpoint calculates and returns the mean value of a security\'s open, high, low, and close prices. This endpoint provides a straightforward metric to assess the overall price level of a security over a specified period.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesAvgPrice(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesAvgPrice200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesBBands**
> GetTimeSeriesBBands200Response getTimeSeriesBBands()

The Bollinger Bands (BBANDS) endpoint calculates and returns three key data points: an upper band, a lower band, and a simple moving average (SMA) for a specified financial instrument. These bands are used to assess market volatility by showing how far prices deviate from the SMA. This information helps users identify potential price reversals and determine whether an asset is overbought or oversold.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 20)
let sd: number; //Number of standard deviations. Must be at least `1` (optional) (default to 2)
let maType: MaTypeEnum; //The type of moving average used (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesBBands(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    sd,
    maType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 20|
| **sd** | [**number**] | Number of standard deviations. Must be at least &#x60;1&#x60; | (optional) defaults to 2|
| **maType** | **MaTypeEnum** | The type of moving average used | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesBBands200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesBeta**
> GetTimeSeriesBeta200Response getTimeSeriesBeta()

The Beta Indicator endpoint provides data on a security\'s sensitivity to market movements by comparing its price changes to a benchmark index. It returns the beta value, which quantifies the systematic risk of the security relative to the market. This information is useful for evaluating how much a security\'s price is expected to move in relation to market changes.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType1: SeriesTypeEnum; //Price type used as the first part of technical indicator (optional) (default to undefined)
let seriesType2: SeriesTypeEnum; //Price type used as the second part of technical indicator (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesBeta(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType1,
    seriesType2,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType1** | **SeriesTypeEnum** | Price type used as the first part of technical indicator | (optional) defaults to undefined|
| **seriesType2** | **SeriesTypeEnum** | Price type used as the second part of technical indicator | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesBeta200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesBop**
> GetTimeSeriesBop200Response getTimeSeriesBop()

The Balance of Power (BOP) endpoint provides data on the buying and selling pressure of a security by analyzing its open, high, low, and close prices. It returns numerical values that help users detect shifts in market sentiment and potential price movements.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesBop(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesBop200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesCci**
> GetTimeSeriesCci200Response getTimeSeriesCci()

The Commodity Channel Index (CCI) endpoint provides data on the CCI values for a specified security, helping users detect potential price reversals by identifying overbought or oversold conditions. It returns a series of CCI values calculated over a specified time period, allowing users to assess the momentum of a security relative to its average price range.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 20)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesCci(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 20|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesCci200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesCeil**
> GetTimeSeriesCeil200Response getTimeSeriesCeil()

The Ceiling (CEIL) endpoint rounds each value in the input data series up to the nearest whole number. It returns a series where each original data point is adjusted to its ceiling value, which can be useful for precise calculations or when integrating with other technical indicators that require integer inputs.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesCeil(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesCeil200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesCmo**
> GetTimeSeriesCmo200Response getTimeSeriesCmo()

The Chande Momentum Oscillator (CMO) endpoint provides data on the momentum of a security by calculating the relative strength of recent price movements. It returns a numerical value indicating whether a security is potentially overbought or oversold, assisting users in identifying possible trend reversals.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesCmo(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesCmo200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesCoppock**
> GetTimeSeriesCoppock200Response getTimeSeriesCoppock()

The Coppock Curve is a momentum oscillator used to detect potential long-term trend reversals in financial markets. It returns the calculated values of this indicator over a specified period, allowing users to identify when a security\'s price may be shifting from a downtrend to an uptrend. This endpoint is particularly useful for analyzing securities in bottoming markets.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let wmaPeriod: number; //Number of periods for weighted moving average. Takes values in the range from `1` to `800` (optional) (default to 10)
let longRocPeriod: number; //Number of periods for long term rate of change. Takes values in the range from `1` to `800` (optional) (default to 14)
let shortRocPeriod: number; //Number of periods for short term rate of change. Takes values in the range from `1` to `800` (optional) (default to 11)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesCoppock(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    wmaPeriod,
    longRocPeriod,
    shortRocPeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **wmaPeriod** | [**number**] | Number of periods for weighted moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 10|
| **longRocPeriod** | [**number**] | Number of periods for long term rate of change. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **shortRocPeriod** | [**number**] | Number of periods for short term rate of change. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 11|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesCoppock200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesCorrel**
> GetTimeSeriesCorrel200Response getTimeSeriesCorrel()

The Correlation (CORREL) endpoint calculates the statistical relationship between two securities over a specified time period, returning a correlation coefficient. This coefficient ranges from -1 to 1, indicating the strength and direction of their linear relationship. A value close to 1 suggests a strong positive correlation, while a value near -1 indicates a strong negative correlation. This data is useful for identifying securities that move together or in opposite directions, aiding in strategies like diversification or pairs trading.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType1: SeriesTypeEnum; //Price type used as the first part of technical indicator (optional) (default to undefined)
let seriesType2: SeriesTypeEnum; //Price type used as the second part of technical indicator (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesCorrel(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType1,
    seriesType2,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType1** | **SeriesTypeEnum** | Price type used as the first part of technical indicator | (optional) defaults to undefined|
| **seriesType2** | **SeriesTypeEnum** | Price type used as the second part of technical indicator | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesCorrel200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesCrsi**
> GetTimeSeriesCrsi200Response getTimeSeriesCrsi()

The Connors Relative Strength Index (CRSI) endpoint provides a detailed analysis of stock momentum by combining three components: the Relative Strength Index, the Rate of Change, and the Up/Down Length. This endpoint returns a numerical value that helps identify potential trend reversals and momentum shifts in a security\'s price. Ideal for traders seeking to refine entry and exit points, the CRSI offers a nuanced view of market conditions beyond traditional RSI indicators.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let rsiPeriod: number; //Number of periods for RSI used to calculate price momentum. Takes values in the range from `1` to `800` (optional) (default to 3)
let upDownLength: number; //Number of periods for RSI used to calculate up/down trend. Takes values in the range from `1` to `800` (optional) (default to 2)
let percentRankPeriod: number; //Number of periods used to calculate PercentRank. Takes values in the range from `1` to `800` (optional) (default to 100)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesCrsi(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    rsiPeriod,
    upDownLength,
    percentRankPeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **rsiPeriod** | [**number**] | Number of periods for RSI used to calculate price momentum. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 3|
| **upDownLength** | [**number**] | Number of periods for RSI used to calculate up/down trend. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 2|
| **percentRankPeriod** | [**number**] | Number of periods used to calculate PercentRank. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 100|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesCrsi200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesDema**
> GetTimeSeriesDema200Response getTimeSeriesDema()

The Double Exponential Moving Average (DEMA) endpoint provides a data series that calculates a moving average with reduced lag by emphasizing recent price data. This endpoint returns time-series data that includes the DEMA values for a specified financial instrument, allowing users to track price trends and identify potential trading opportunities.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesDema(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesDema200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesDiv**
> GetTimeSeriesDiv200Response getTimeSeriesDiv()

The Division (DIV) endpoint calculates the result of dividing one data series by another, providing a normalized output. It is commonly used to combine or adjust multiple technical indicators or price data for comparative analysis. This endpoint returns the division results as a time series, allowing users to easily interpret and utilize the normalized data in their financial models or charts.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType1: SeriesTypeEnum; //Price type used as the first part of technical indicator (optional) (default to undefined)
let seriesType2: SeriesTypeEnum; //Price type used as the second part of technical indicator (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesDiv(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType1,
    seriesType2,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType1** | **SeriesTypeEnum** | Price type used as the first part of technical indicator | (optional) defaults to undefined|
| **seriesType2** | **SeriesTypeEnum** | Price type used as the second part of technical indicator | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesDiv200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesDpo**
> GetTimeSeriesDpo200Response getTimeSeriesDpo()

The Detrended Price Oscillator (DPO) endpoint calculates and returns the DPO values for a specified financial instrument over a given time period. This endpoint helps traders by highlighting short-term price cycles and identifying potential overbought or oversold conditions without the influence of long-term trends. The response includes a series of DPO values, which can be used to assess price momentum and cyclical patterns in the market.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let centered: boolean; //Specifies if there should be a shift to match the current price (optional) (default to false)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesDpo(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    centered,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **centered** | [**boolean**] | Specifies if there should be a shift to match the current price | (optional) defaults to false|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesDpo200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesDx**
> GetTimeSeriesDx200Response getTimeSeriesDx()

Retrieve the Directional Movement Index (DX) values for a given security to assess the strength of its positive and negative price movements. This endpoint provides a time series of DX values, which are useful for evaluating the momentum and trend direction of the security over a specified period.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesDx(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesDx200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesEma**
> GetTimeSeriesEma200Response getTimeSeriesEma()

The Exponential Moving Average (EMA) endpoint calculates the EMA for a specified financial instrument over a given time period. It returns a time series of EMA values, which highlight recent price trends by weighting recent data more heavily. This is useful for traders seeking to identify trend directions and potential trade opportunities based on recent price movements.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesEma(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesEma200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesExp**
> GetTimeSeriesExp200Response getTimeSeriesExp()

The Exponential (EXP) Indicator endpoint computes the exponential value of a specified input, providing a numerical result that is commonly applied in complex mathematical and financial computations.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesExp(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesExp200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesFloor**
> GetTimeSeriesFloor200Response getTimeSeriesFloor()

The Floor (FLOOR) endpoint processes numerical input data by rounding each value down to the nearest integer. It returns a series of adjusted data points that can be used for further calculations or combined with other datasets. This endpoint is useful for users needing to simplify data by removing decimal precision, aiding in scenarios where integer values are required.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesFloor(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesFloor200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesHeikinashiCandles**
> GetTimeSeriesHeikinashiCandles200Response getTimeSeriesHeikinashiCandles()

The heikinashi candles endpoint provides smoothed candlestick data by averaging price information to reduce market noise. It returns a series of Heikin Ashi candles, which include open, high, low, and close values, making it easier to identify trends and potential reversals in asset prices. This endpoint is useful for traders and analysts seeking a clearer view of market trends without the volatility present in traditional candlestick charts.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesHeikinashiCandles(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesHeikinashiCandles200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesHlc3**
> GetTimeSeriesHlc3200Response getTimeSeriesHlc3()

The High, Low, Close Average (HLC3) endpoint calculates and returns the average of a security\'s high, low, and close prices for a specified period. This endpoint provides a straightforward metric to assess price trends, helping users quickly identify the average price level of a security over time.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesHlc3(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesHlc3200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesHtDcPeriod**
> GetTimeSeriesHtDcPeriod200Response getTimeSeriesHtDcPeriod()

The Hilbert Transform Dominant Cycle Period (HT_DCPERIOD) endpoint calculates the dominant cycle length of a financial instrument\'s price data. It returns a numerical value representing the cycle period, which traders can use to identify prevailing market cycles and adjust their trading strategies accordingly.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesHtDcPeriod(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesHtDcPeriod200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesHtDcPhase**
> GetTimeSeriesHtDcPhase200Response getTimeSeriesHtDcPhase()

The Hilbert Transform Dominant Cycle Phase (HT_DCPHASE) endpoint provides the current phase of the dominant market cycle for a given financial instrument. It returns numerical data indicating the phase angle, which can be used by traders to identify potential market entry and exit points based on cyclical patterns.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesHtDcPhase(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesHtDcPhase200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesHtPhasor**
> GetTimeSeriesHtPhasor200Response getTimeSeriesHtPhasor()

The Hilbert Transform Phasor Components (HT_PHASOR) endpoint analyzes a price series to return two key components: in-phase and quadrature. These components help identify cyclical patterns and the direction of trends in the data. Use this endpoint to gain precise insights into the timing and strength of market cycles, enhancing your ability to track and predict price movements.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesHtPhasor(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesHtPhasor200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesHtSine**
> GetTimeSeriesHtSine200Response getTimeSeriesHtSine()

The Hilbert Transform Sine Wave (HT_SINE) endpoint provides sine and cosine wave components derived from the dominant market cycle. This data helps traders pinpoint potential market turning points and assess trend directions by analyzing cyclical patterns.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesHtSine(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesHtSine200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesHtTrendMode**
> GetTimeSeriesHtTrendMode200Response getTimeSeriesHtTrendMode()

The Hilbert Transform Trend vs Cycle Mode (HT_TRENDMODE) endpoint identifies whether a market is in a trending or cyclical phase. It returns data indicating the current market phase, allowing users to adjust their trading strategies based on the prevailing conditions.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesHtTrendMode(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesHtTrendMode200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesHtTrendline**
> GetTimeSeriesHtTrendline200Response getTimeSeriesHtTrendline()

The Hilbert Transform Instantaneous Trendline (HT_TRENDLINE) endpoint provides a smoothed moving average that aligns with the dominant market cycle. It returns data points that help traders identify current market trends and determine potential entry or exit points in trading.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesHtTrendline(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesHtTrendline200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesIchimoku**
> GetTimeSeriesIchimoku200Response getTimeSeriesIchimoku()

The Ichimoku Cloud endpoint provides data on the Ichimoku Kinko Hyo indicator, offering insights into trend direction, support and resistance levels, and potential entry and exit points. It returns key components such as the Tenkan-sen, Kijun-sen, Senkou Span A, Senkou Span B, and Chikou Span. This data helps users evaluate market trends and identify strategic trading opportunities.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let conversionLinePeriod: number; //The time period used for generating the conversation line. Takes values in the range from `1` to `800` (optional) (default to 9)
let baseLinePeriod: number; //The time period used for generating the base line. Takes values in the range from `1` to `800` (optional) (default to 26)
let leadingSpanBPeriod: number; //The time period used for generating the leading span B line. Takes values in the range from `1` to `800` (optional) (default to 52)
let laggingSpanPeriod: number; //The time period used for generating the lagging span line. Takes values in the range from `1` to `800` (optional) (default to 26)
let includeAheadSpanPeriod: boolean; //Indicates whether to include ahead span period (optional) (default to true)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesIchimoku(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    conversionLinePeriod,
    baseLinePeriod,
    leadingSpanBPeriod,
    laggingSpanPeriod,
    includeAheadSpanPeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **conversionLinePeriod** | [**number**] | The time period used for generating the conversation line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **baseLinePeriod** | [**number**] | The time period used for generating the base line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 26|
| **leadingSpanBPeriod** | [**number**] | The time period used for generating the leading span B line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 52|
| **laggingSpanPeriod** | [**number**] | The time period used for generating the lagging span line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 26|
| **includeAheadSpanPeriod** | [**boolean**] | Indicates whether to include ahead span period | (optional) defaults to true|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesIchimoku200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesKama**
> GetTimeSeriesKama200Response getTimeSeriesKama()

The Kaufman Adaptive Moving Average (KAMA) endpoint calculates the KAMA for a specified financial instrument, returning a time series of values that reflect the average price adjusted for market volatility. This endpoint helps users identify trends by smoothing out price fluctuations while remaining sensitive to significant price movements.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesKama(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesKama200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesKeltner**
> GetTimeSeriesKeltner200Response getTimeSeriesKeltner()

The Keltner Channel endpoint provides data for a volatility-based technical indicator that combines the Exponential Moving Average (EMA) and the Average True Range (ATR) to form a channel around a security\'s price. This endpoint returns the upper, middle, and lower bands of the channel, which can be used to identify potential overbought or oversold conditions, assess trend direction, and detect possible price breakouts.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 20)
let atrTimePeriod: number; //The time period used for calculating the Average True Range. Takes values in the range from `1` to `800` (optional) (default to 10)
let multiplier: number; //The factor used to adjust the indicator\'s sensitivity (optional) (default to 2)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let maType: MaTypeEnum; //The type of moving average used (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesKeltner(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    atrTimePeriod,
    multiplier,
    seriesType,
    maType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 20|
| **atrTimePeriod** | [**number**] | The time period used for calculating the Average True Range. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 10|
| **multiplier** | [**number**] | The factor used to adjust the indicator\&#39;s sensitivity | (optional) defaults to 2|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **maType** | **MaTypeEnum** | The type of moving average used | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesKeltner200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesKst**
> GetTimeSeriesKst200Response getTimeSeriesKst()

The Know Sure Thing (KST) endpoint provides a momentum oscillator that combines four smoothed rates of change into a single trend-following indicator. This endpoint returns data that helps users identify potential trend reversals, as well as overbought or oversold conditions in the market.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let rocPeriod1: number; //The time period for the first Rate of Change calculation. (optional) (default to 10)
let rocPeriod2: number; //The time period for the second Rate of Change calculation. (optional) (default to 15)
let rocPeriod3: number; //The time period for the third Rate of Change calculation. (optional) (default to 20)
let rocPeriod4: number; //The time period for the forth Rate of Change calculation. (optional) (default to 30)
let smaPeriod1: number; //The time period for the first Simple Moving Average. (optional) (default to 10)
let smaPeriod2: number; //The time period for the second Simple Moving Average. (optional) (default to 10)
let smaPeriod3: number; //The time period for the third Simple Moving Average. (optional) (default to 10)
let smaPeriod4: number; //The time period for the forth Simple Moving Average. (optional) (default to 15)
let signalPeriod: number; //The time period used for generating the signal line. (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesKst(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    rocPeriod1,
    rocPeriod2,
    rocPeriod3,
    rocPeriod4,
    smaPeriod1,
    smaPeriod2,
    smaPeriod3,
    smaPeriod4,
    signalPeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **rocPeriod1** | [**number**] | The time period for the first Rate of Change calculation. | (optional) defaults to 10|
| **rocPeriod2** | [**number**] | The time period for the second Rate of Change calculation. | (optional) defaults to 15|
| **rocPeriod3** | [**number**] | The time period for the third Rate of Change calculation. | (optional) defaults to 20|
| **rocPeriod4** | [**number**] | The time period for the forth Rate of Change calculation. | (optional) defaults to 30|
| **smaPeriod1** | [**number**] | The time period for the first Simple Moving Average. | (optional) defaults to 10|
| **smaPeriod2** | [**number**] | The time period for the second Simple Moving Average. | (optional) defaults to 10|
| **smaPeriod3** | [**number**] | The time period for the third Simple Moving Average. | (optional) defaults to 10|
| **smaPeriod4** | [**number**] | The time period for the forth Simple Moving Average. | (optional) defaults to 15|
| **signalPeriod** | [**number**] | The time period used for generating the signal line. | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesKst200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesLinearReg**
> GetTimeSeriesLinearReg200Response getTimeSeriesLinearReg()

The Linear Regression endpoint (LINEARREG) calculates the best-fit straight line through a series of financial data points. It returns the slope and intercept values of this line, allowing users to determine the overall direction of a market trend and identify potential support or resistance levels.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesLinearReg(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesLinearReg200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesLinearRegAngle**
> GetTimeSeriesLinearRegAngle200Response getTimeSeriesLinearRegAngle()

The Linear Regression Angle endpoint (LINEARREGANGLE) calculates the angle of the linear regression line for a given time series of stock prices. It returns the slope of the trend line, expressed in degrees, which helps users identify the direction and steepness of a trend over a specified period. This data is useful for detecting upward or downward trends in asset prices.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesLinearRegAngle(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesLinearRegAngle200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesLinearRegIntercept**
> GetTimeSeriesLinearRegIntercept200Response getTimeSeriesLinearRegIntercept()

The Linear Regression Intercept endpoint (LINEARREGINTERCEPT) calculates the y-intercept of a linear regression line for a given dataset. It returns the value where the regression line crosses the y-axis, providing a numerical reference point for understanding the starting position of a trend over a specified period. This can be useful for users needing to establish baseline values in their data analysis.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesLinearRegIntercept(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesLinearRegIntercept200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesLinearRegSlope**
> GetTimeSeriesLinearRegSlope200Response getTimeSeriesLinearRegSlope()

The Linear Regression Slope endpoint (LINEARREGSLOPE) calculates the slope of a linear regression line for a given dataset, reflecting the rate of change in the data trend over a specified period. It returns a numerical value representing this slope, which can be used to assess the direction and strength of the trend in the dataset.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesLinearRegSlope(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesLinearRegSlope200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesLn**
> GetTimeSeriesLn200Response getTimeSeriesLn()

The Natural Logarithm (LN) endpoint computes the natural logarithm of a specified input value, returning a numerical result. This endpoint is useful for users needing to perform logarithmic transformations on data, which can be applied in various financial calculations and advanced mathematical analyses.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesLn(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesLn200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesLog10**
> GetTimeSeriesLog10200Response getTimeSeriesLog10()

The Base-10 Logarithm (LOG10) endpoint computes the base-10 logarithm of a specified input value. It returns a numerical result that represents the power to which the number 10 must be raised to obtain the input value. This endpoint is useful for transforming data into a logarithmic scale, which can simplify the analysis of exponential growth patterns or compress large ranges of data in financial calculations.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesLog10(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesLog10200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMa**
> GetTimeSeriesMa200Response getTimeSeriesMa()

The Moving Average (MA) endpoint provides the average price of a security over a specified time frame, offering a smoothed representation of price data. This endpoint returns the calculated moving average values, which can assist users in identifying price trends and potential support or resistance levels in the market.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let maType: MaTypeEnum; //The type of moving average used (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMa(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    maType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **maType** | **MaTypeEnum** | The type of moving average used | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMa200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMacd**
> GetTimeSeriesMacd200Response getTimeSeriesMacd()

This endpoint calculates the Moving Average Convergence Divergence (MACD) for a specified financial instrument. It returns the MACD line, signal line, and histogram values, which help users identify potential trend reversals and trading opportunities by analyzing the relationship between two moving averages.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let fastPeriod: number; //Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
let slowPeriod: number; //Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
let signalPeriod: number; //The time period used for generating the signal line. (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMacd(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    fastPeriod,
    slowPeriod,
    signalPeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **fastPeriod** | [**number**] | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 12|
| **slowPeriod** | [**number**] | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 26|
| **signalPeriod** | [**number**] | The time period used for generating the signal line. | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMacd200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMacdExt**
> GetTimeSeriesMacdExt200Response getTimeSeriesMacdExt()

The Moving Average Convergence Divergence Extension (MACDEXT) endpoint provides a customizable version of the MACD indicator, allowing users to specify different moving average types and parameters. It returns data that includes the MACD line, signal line, and histogram values, tailored to the user\'s chosen settings. This endpoint is useful for traders who require flexibility in analyzing price trends and momentum by adjusting the calculation methods to fit their specific trading strategies.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let fastPeriod: number; //Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
let fastMaType: MaTypeEnum; //The type of fast moving average used in the calculation. (optional) (default to undefined)
let slowPeriod: number; //Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
let slowMaType: MaTypeEnum; //The type of slow moving average used in the calculation. (optional) (default to undefined)
let signalPeriod: number; //The time period used for generating the signal line. (optional) (default to 9)
let signalMaType: MaTypeEnum; //The type of fast moving average used for generating the signal line. (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMacdExt(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    fastPeriod,
    fastMaType,
    slowPeriod,
    slowMaType,
    signalPeriod,
    signalMaType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **fastPeriod** | [**number**] | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 12|
| **fastMaType** | **MaTypeEnum** | The type of fast moving average used in the calculation. | (optional) defaults to undefined|
| **slowPeriod** | [**number**] | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 26|
| **slowMaType** | **MaTypeEnum** | The type of slow moving average used in the calculation. | (optional) defaults to undefined|
| **signalPeriod** | [**number**] | The time period used for generating the signal line. | (optional) defaults to 9|
| **signalMaType** | **MaTypeEnum** | The type of fast moving average used for generating the signal line. | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMacdExt200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMacdSlope**
> GetTimeSeriesMacdSlope200Response getTimeSeriesMacdSlope()

The Moving Average Convergence Divergence (MACD) Slope endpoint provides the rate of change of the MACD line for a given security. It returns data on how quickly the MACD line is rising or falling, offering insights into the momentum shifts in the security\'s price. This information is useful for traders looking to gauge the speed of price movements and potential trend reversals.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let fastPeriod: number; //Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
let slowPeriod: number; //Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
let signalPeriod: number; //The time period used for generating the signal line. (optional) (default to 9)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMacdSlope(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    fastPeriod,
    slowPeriod,
    signalPeriod,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **fastPeriod** | [**number**] | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 12|
| **slowPeriod** | [**number**] | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 26|
| **signalPeriod** | [**number**] | The time period used for generating the signal line. | (optional) defaults to 9|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMacdSlope200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMama**
> GetTimeSeriesMama200Response getTimeSeriesMama()

The MESA Adaptive Moving Average (MAMA) endpoint calculates a moving average that adjusts to the dominant market cycle, offering a balance between quick response to price changes and noise reduction. It returns data that includes the adaptive moving average values, which can be used to identify trends and potential reversal points.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let fastLimit: number; //The limit for the fast moving average. (optional) (default to 0.5)
let slowLimit: number; //The limit for the slow moving average. (optional) (default to 0.05)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMama(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    fastLimit,
    slowLimit,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **fastLimit** | [**number**] | The limit for the fast moving average. | (optional) defaults to 0.5|
| **slowLimit** | [**number**] | The limit for the slow moving average. | (optional) defaults to 0.05|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMama200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMax**
> GetTimeSeriesMax200Response getTimeSeriesMax()

The Maximum (MAX) endpoint calculates and returns the highest value within a specified data series over a given period. This endpoint is useful for identifying potential resistance levels or detecting extreme price movements in financial data.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMax(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMax200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMaxIndex**
> GetTimeSeriesMaxIndex200Response getTimeSeriesMaxIndex()

The Maximum Index (MAXINDEX) endpoint identifies the position of the highest value within a specified data series over a given time frame. It returns the index where the peak value occurs, allowing users to pinpoint when the maximum price or value was reached in the series. This is useful for tracking the timing of significant peaks in financial data.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMaxIndex(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMaxIndex200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMcGinleyDynamic**
> GetTimeSeriesMcGinleyDynamic200Response getTimeSeriesMcGinleyDynamic()

This endpoint calculates the McGinley Dynamic (MCGINLEY_DYNAMIC) indicator, which provides a refined moving average that adapts to market volatility. This endpoint returns data that reflects smoother price trends and identifies potential support or resistance levels more accurately than traditional moving averages. It is useful for users seeking to track price movements with reduced lag and enhanced responsiveness to market changes.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMcGinleyDynamic(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMcGinleyDynamic200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMedPrice**
> GetTimeSeriesMedPrice200Response getTimeSeriesMedPrice()

The Median Price (MEDPRICE) endpoint calculates and returns the average of the high and low prices of a security for a specified period. This endpoint provides a simplified view of price movements, helping users quickly assess price trends by focusing on the midpoint of price action.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMedPrice(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMedPrice200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMfi**
> GetTimeSeriesMfi200Response getTimeSeriesMfi()

The Money Flow Index (MFI) endpoint provides a volume-weighted momentum oscillator that quantifies buying and selling pressure by analyzing positive and negative money flow. It returns data indicating potential overbought or oversold conditions in a financial asset, aiding users in understanding market trends and price movements.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMfi(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMfi200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMidPoint**
> GetTimeSeriesMidPoint200Response getTimeSeriesMidPoint()

The Midpoint (MIDPOINT) endpoint calculates the average value between the highest and lowest prices of a financial instrument over a specified period. It returns a time series of midpoint values, which can help users identify price trends and smooth out short-term fluctuations in the data.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMidPoint(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMidPoint200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMidPrice**
> GetTimeSeriesMidPrice200Response getTimeSeriesMidPrice()

The Midprice (MIDPRICE) endpoint calculates and returns the average of a financial instrument\'s highest and lowest prices over a specified time period. This data provides a smoothed representation of price movements, helping users identify potential support or resistance levels in the market.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMidPrice(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMidPrice200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMin**
> GetTimeSeriesMin200Response getTimeSeriesMin()

The Minimum (MIN) Indicator endpoint provides the lowest value of a specified data series over a chosen time period. This endpoint is useful for identifying potential support levels or detecting extreme price movements in financial data.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMin(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMin200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMinIndex**
> GetTimeSeriesMinIndex200Response getTimeSeriesMinIndex()

The Minimum Index (MININDEX) endpoint identifies the position of the lowest value within a specified data series over a given time frame. It returns the index number corresponding to the earliest occurrence of this minimum value. This is useful for pinpointing when the lowest price or value occurred in a dataset, aiding in time-based analysis of data trends.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMinIndex(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMinIndex200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMinMax**
> GetTimeSeriesMinMax200Response getTimeSeriesMinMax()

The Minimum and Maximum (MINMAX) endpoint identifies the lowest and highest values within a specified time frame for a given data series. It returns these extreme values, which can be used to detect potential support and resistance levels or significant price fluctuations in the data.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMinMax(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMinMax200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMinMaxIndex**
> GetTimeSeriesMinMaxIndex200Response getTimeSeriesMinMaxIndex()

The Minimum and Maximum Index (MINMAXINDEX) endpoint identifies the positions of the lowest and highest values within a specified data series period. It returns indices that indicate when these extreme values occur, allowing users to pinpoint significant price changes over time.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMinMaxIndex(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMinMaxIndex200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMinusDI**
> GetTimeSeriesMinusDI200Response getTimeSeriesMinusDI()

The Minus Directional Indicator (MINUS_DI) endpoint calculates and returns the strength of a security\'s downward price movement over a specified period. This data is useful for traders and analysts looking to identify bearish trends and assess the intensity of price declines in financial markets.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMinusDI(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMinusDI200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMinusDM**
> GetTimeSeriesMinusDM200Response getTimeSeriesMinusDM()

The Minus Directional Movement endpoint (MINUS_DM) calculates the downward price movement of a security over a specified period. It returns a series of values indicating the strength of downward trends, useful for traders to identify potential selling opportunities or confirm bearish market conditions.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMinusDM(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMinusDM200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMom**
> GetTimeSeriesMom200Response getTimeSeriesMom()

The Momentum (MOM) endpoint provides data on the rate of change in a security\'s price over a user-defined period. It returns a series of numerical values indicating the speed and direction of the price movement, which can help users detect emerging trends or potential reversals in the market.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMom(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMom200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesMult**
> GetTimeSeriesMult200Response getTimeSeriesMult()

The Multiplication (MULT) endpoint calculates the product of two input data series, returning a new data series that represents the element-wise multiplication of the inputs. This is useful for combining or adjusting technical indicators or price data to create custom metrics or to normalize values across different scales.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType1: SeriesTypeEnum; //Price type used as the first part of technical indicator (optional) (default to undefined)
let seriesType2: SeriesTypeEnum; //Price type used as the second part of technical indicator (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesMult(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType1,
    seriesType2,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType1** | **SeriesTypeEnum** | Price type used as the first part of technical indicator | (optional) defaults to undefined|
| **seriesType2** | **SeriesTypeEnum** | Price type used as the second part of technical indicator | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesMult200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesNatr**
> GetTimeSeriesNatr200Response getTimeSeriesNatr()

The Normalized Average True Range (NATR) endpoint provides a volatility indicator that calculates the average range of price movement over a specified period, expressed as a percentage of the security\'s price. This data allows users to compare volatility levels across different securities easily. The endpoint returns a time series of NATR values, which can be used to assess and compare the price volatility of various financial instruments.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesNatr(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesNatr200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesObv**
> GetTimeSeriesObv200Response getTimeSeriesObv()

The On Balance Volume (OBV) endpoint provides a time series of the OBV indicator, which calculates cumulative volume to reflect buying and selling pressure over time. This endpoint returns data that helps users track volume trends in relation to price movements, aiding in the identification of potential trend continuations or reversals in a security\'s price.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesObv(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesObv200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesPercentB**
> GetTimeSeriesPercentB200Response getTimeSeriesPercentB()

The Percent B (%B) endpoint calculates and returns the %B value, which indicates the position of a security\'s price relative to its Bollinger Bands. This data helps users determine if a security is near the upper or lower band, potentially signaling overbought or oversold conditions.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let sd: number; //The standard deviation applied in the calculation.  Must be at least `1` (optional) (default to 2)
let maType: MaTypeEnum; //The type of moving average used (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesPercentB(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    sd,
    maType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **sd** | [**number**] | The standard deviation applied in the calculation.  Must be at least &#x60;1&#x60; | (optional) defaults to 2|
| **maType** | **MaTypeEnum** | The type of moving average used | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesPercentB200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesPivotPointsHL**
> GetTimeSeriesPivotPointsHL200Response getTimeSeriesPivotPointsHL()

The Pivot Points High Low (PIVOT_POINTS_HL) endpoint calculates key support and resistance levels for a security by analyzing its highest and lowest prices over a specified period. This endpoint returns data that includes pivot points, support levels, and resistance levels, which can be used to identify potential price reversal zones and optimize trade entry and exit strategies.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 10)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesPivotPointsHL(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 10|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesPivotPointsHL200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesPlusDI**
> GetTimeSeriesPlusDI200Response getTimeSeriesPlusDI()

The Plus Directional Indicator endpoint (/plus_di) provides data on the strength of a security\'s upward price movement by calculating the Plus Directional Indicator (PLUS_DI). It returns a time series of PLUS_DI values, which can be used to assess the intensity of upward trends in a security\'s price over a specified period.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesPlusDI(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesPlusDI200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesPlusDM**
> GetTimeSeriesPlusDM200Response getTimeSeriesPlusDM()

The Plus Directional Movement (PLUS_DM) endpoint calculates the upward price movement of a financial security over a specified period. It returns numerical values representing the magnitude of upward price changes, which can be used to assess the strength of an uptrend. This data is essential for traders and analysts who need to evaluate the bullish momentum of a security.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesPlusDM(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesPlusDM200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesPpo**
> GetTimeSeriesPpo200Response getTimeSeriesPpo()

The Percentage Price Oscillator (PPO) endpoint calculates the percentage difference between two specified moving averages of a financial instrument\'s price. It returns data that includes the PPO values, which traders can use to identify potential trend reversals and generate trading signals.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let fastPeriod: number; //Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
let slowPeriod: number; //Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
let maType: MaTypeEnum; //The type of moving average used (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesPpo(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    fastPeriod,
    slowPeriod,
    maType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **fastPeriod** | [**number**] | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 12|
| **slowPeriod** | [**number**] | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 26|
| **maType** | **MaTypeEnum** | The type of moving average used | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesPpo200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesRoc**
> GetTimeSeriesRoc200Response getTimeSeriesRoc()

The Rate of Change (ROC) endpoint calculates the percentage change in a security\'s price over a defined period, returning a time series of ROC values. This data helps users track momentum by showing how quickly prices are changing, which can be useful for identifying potential price movements.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesRoc(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesRoc200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesRocp**
> GetTimeSeriesRocp200Response getTimeSeriesRocp()

The Rate of Change Percentage (ROCP) endpoint calculates and returns the percentage change in the price of a financial security over a user-defined period. This data helps users identify shifts in price momentum and potential trend reversals by providing a clear numerical representation of how much the price has increased or decreased in percentage terms.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesRocp(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesRocp200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesRocr**
> GetTimeSeriesRocr200Response getTimeSeriesRocr()

The Rate of Change Ratio (ROCR) endpoint calculates and returns the ratio of a security\'s current price to its price from a specified number of periods ago. This data helps users track price momentum and identify potential trend reversals by providing a clear numerical value that reflects price changes over time.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesRocr(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesRocr200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesRocr100**
> GetTimeSeriesRocr100200Response getTimeSeriesRocr100()

The Rate of Change Ratio 100 (ROCR100) endpoint calculates the percentage change in a security\'s price over a specified period, expressed as a ratio to 100. It returns data that highlights the momentum of the price movement and identifies potential trend reversals. This endpoint is useful for users looking to assess the strength and direction of a security\'s price trend over time.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesRocr100(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesRocr100200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesRsi**
> GetTimeSeriesRsi200Response getTimeSeriesRsi()

The Relative Strength Index (RSI) endpoint provides data on the RSI values for a specified financial instrument over a given period. It returns a series of RSI values, which indicate the momentum of price movements and help identify potential overbought or oversold conditions. This data is useful for traders looking to assess the strength of price trends and anticipate possible trend reversals.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesRsi(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesRsi200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesRvol**
> GetTimeSeriesRvol200Response getTimeSeriesRvol()

The Relative Volume endpoint (/rvol) provides a ratio comparing a security\'s current trading volume to its average volume over a specified period. This data helps users detect unusual trading activity and assess the strength of price movements, offering insights into potential market breakouts.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesRvol(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesRvol200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesSar**
> GetTimeSeriesSar200Response getTimeSeriesSar()

The Parabolic Stop and Reverse (SAR) endpoint provides data on potential support and resistance levels for a specified security, using its price and time. This endpoint returns numerical values that help traders determine possible entry and exit points in their trading strategies.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let acceleration: number; //The rate of change in the indicator\'s values. (optional) (default to 0.02)
let maximum: number; //The maximum value considered for the indicator calculation. (optional) (default to 0.2)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesSar(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    acceleration,
    maximum,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **acceleration** | [**number**] | The rate of change in the indicator\&#39;s values. | (optional) defaults to 0.02|
| **maximum** | [**number**] | The maximum value considered for the indicator calculation. | (optional) defaults to 0.2|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesSar200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesSarExt**
> GetTimeSeriesSarExt200Response getTimeSeriesSarExt()

The Parabolic SAR Extended (SAREXT) endpoint provides a customizable version of the Parabolic SAR indicator, which is used to identify potential entry and exit points in trading. Users can adjust parameters such as acceleration factors to tailor the indicator to specific trading strategies. The endpoint returns data points indicating potential trend reversals.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let startValue: number; //The initial value for the indicator calculation. (optional) (default to 0)
let offsetOnReverse: number; //The adjustment applied when the indicator\'s direction changes. (optional) (default to 0)
let accelerationLimitLong: number; //The maximum acceleration value for long positions. (optional) (default to 0.02)
let accelerationLong: number; //The acceleration value for long positions. (optional) (default to 0.02)
let accelerationMaxLong: number; //The highest allowed acceleration for long positions. (optional) (default to 0.2)
let accelerationLimitShort: number; //The maximum acceleration value for short positions. (optional) (default to 0.02)
let accelerationShort: number; //The acceleration value for short positions. (optional) (default to 0.02)
let accelerationMaxShort: number; //The highest allowed acceleration for short positions. (optional) (default to 0.2)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesSarExt(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    startValue,
    offsetOnReverse,
    accelerationLimitLong,
    accelerationLong,
    accelerationMaxLong,
    accelerationLimitShort,
    accelerationShort,
    accelerationMaxShort,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **startValue** | [**number**] | The initial value for the indicator calculation. | (optional) defaults to 0|
| **offsetOnReverse** | [**number**] | The adjustment applied when the indicator\&#39;s direction changes. | (optional) defaults to 0|
| **accelerationLimitLong** | [**number**] | The maximum acceleration value for long positions. | (optional) defaults to 0.02|
| **accelerationLong** | [**number**] | The acceleration value for long positions. | (optional) defaults to 0.02|
| **accelerationMaxLong** | [**number**] | The highest allowed acceleration for long positions. | (optional) defaults to 0.2|
| **accelerationLimitShort** | [**number**] | The maximum acceleration value for short positions. | (optional) defaults to 0.02|
| **accelerationShort** | [**number**] | The acceleration value for short positions. | (optional) defaults to 0.02|
| **accelerationMaxShort** | [**number**] | The highest allowed acceleration for short positions. | (optional) defaults to 0.2|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesSarExt200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesSma**
> GetTimeSeriesSma200Response getTimeSeriesSma()

The Simple Moving Average (SMA) endpoint calculates and returns the average price of a security over a user-defined time period. This endpoint provides a series of data points that represent the smoothed price trend, which can help users identify potential price movements and evaluate historical price behavior.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesSma(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesSma200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesSqrt**
> GetTimeSeriesSqrt200Response getTimeSeriesSqrt()

The Square Root (SQRT) endpoint computes the square root of a specified numerical input. It returns a single numerical value representing the square root, which can be used in various mathematical computations or financial models requiring this specific transformation.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesSqrt(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesSqrt200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesStdDev**
> GetTimeSeriesStdDev200Response getTimeSeriesStdDev()

The Standard Deviation (STDDEV) endpoint calculates the dispersion of a financial instrument\'s price data from its average value. It returns a numerical value representing the volatility of the asset over a specified period. This endpoint is useful for traders and analysts to assess price variability and identify periods of high or low volatility in the market.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let sd: number; //The standard deviation applied in the calculation. (optional) (default to 2)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesStdDev(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    sd,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **sd** | [**number**] | The standard deviation applied in the calculation. | (optional) defaults to 2|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesStdDev200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesStoch**
> GetTimeSeriesStoch200Response getTimeSeriesStoch()

The Stochastic Oscillator endpoint provides data on a momentum indicator that evaluates a security\'s closing price relative to its price range over a specified timeframe. It returns values indicating potential overbought or oversold conditions, aiding in identifying possible trend reversals. Users receive the %K and %D values, which are essential for analyzing the momentum and potential turning points in the market.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let fastKPeriod: number; //The time period for the fast %K line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 14)
let slowKPeriod: number; //The time period for the slow %K line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 1)
let slowDPeriod: number; //The time period for the slow %D line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 3)
let slowKmaType: MaTypeEnum; //The type of slow %K Moving Average used. Default is SMA. (optional) (default to undefined)
let slowDmaType: MaTypeEnum; //The type of slow Displaced Moving Average used. Default is SMA. (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesStoch(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    fastKPeriod,
    slowKPeriod,
    slowDPeriod,
    slowKmaType,
    slowDmaType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **fastKPeriod** | [**number**] | The time period for the fast %K line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **slowKPeriod** | [**number**] | The time period for the slow %K line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 1|
| **slowDPeriod** | [**number**] | The time period for the slow %D line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 3|
| **slowKmaType** | **MaTypeEnum** | The type of slow %K Moving Average used. Default is SMA. | (optional) defaults to undefined|
| **slowDmaType** | **MaTypeEnum** | The type of slow Displaced Moving Average used. Default is SMA. | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesStoch200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesStochF**
> GetTimeSeriesStochF200Response getTimeSeriesStochF()

The Stochastic Fast (STOCHF) endpoint calculates the fast version of the Stochastic Oscillator, providing data on the momentum of a financial instrument by comparing a particular closing price to a range of its prices over a specified period. This endpoint returns the %K and %D values, which are used to identify potential overbought or oversold conditions in the market. It is useful for traders who need quick, responsive insights into price movements, although it may generate more false signals due to its sensitivity.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let fastKPeriod: number; //The time period for the fast %K line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 14)
let fastDPeriod: number; //The time period for the fast %D line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 3)
let fastDmaType: MaTypeEnum; //The type of fast Displaced Moving Average used. (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesStochF(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    fastKPeriod,
    fastDPeriod,
    fastDmaType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **fastKPeriod** | [**number**] | The time period for the fast %K line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **fastDPeriod** | [**number**] | The time period for the fast %D line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 3|
| **fastDmaType** | **MaTypeEnum** | The type of fast Displaced Moving Average used. | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesStochF200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesStochRsi**
> GetTimeSeriesStochRsi200Response getTimeSeriesStochRsi()

The Stochastic Relative Strength Index (Stochastic RSI) endpoint calculates the Stochastic RSI values for a given financial instrument, providing data on its momentum and potential price reversals. This endpoint returns time-series data, including the %K and %D lines, which help users identify overbought or oversold conditions. Ideal for traders seeking to refine entry and exit points by analyzing short-term price movements.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeStochrsiEnum; //Specifies the price data type: open, high, low, or close. (optional) (default to undefined)
let rsiLength: number; //Length of period for calculating the RSI component. Takes values in the range from `1` to `800` (optional) (default to 14)
let stochLength: number; //Period length for computing the stochastic oscillator of the RSI. Takes values in the range from `1` to `800` (optional) (default to 14)
let kPeriod: number; //Period for smoothing the %K line. Takes values in the range from `1` to `800` (optional) (default to 3)
let slowKmaType: string; // (optional) (default to undefined)
let dPeriod: number; //Period for smoothing the %D line, which is a moving average of %K. Takes values in the range from `1` to `800` (optional) (default to 3)
let slowDmaType: string; // (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesStochRsi(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    rsiLength,
    stochLength,
    kPeriod,
    slowKmaType,
    dPeriod,
    slowDmaType,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeStochrsiEnum** | Specifies the price data type: open, high, low, or close. | (optional) defaults to undefined|
| **rsiLength** | [**number**] | Length of period for calculating the RSI component. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **stochLength** | [**number**] | Period length for computing the stochastic oscillator of the RSI. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **kPeriod** | [**number**] | Period for smoothing the %K line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 3|
| **slowKmaType** | [**string**] |  | (optional) defaults to undefined|
| **dPeriod** | [**number**] | Period for smoothing the %D line, which is a moving average of %K. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 3|
| **slowDmaType** | [**string**] |  | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesStochRsi200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesSub**
> GetTimeSeriesSub200Response getTimeSeriesSub()

The Subtraction (SUB) endpoint calculates the difference between two input data series, such as technical indicators or price data. It returns a time series of the resulting values, allowing users to compare or normalize data by highlighting the variance between the two series.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType1: SeriesTypeEnum; //Price type used as the first part of technical indicator (optional) (default to undefined)
let seriesType2: SeriesTypeEnum; //Price type used as the second part of technical indicator (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesSub(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType1,
    seriesType2,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType1** | **SeriesTypeEnum** | Price type used as the first part of technical indicator | (optional) defaults to undefined|
| **seriesType2** | **SeriesTypeEnum** | Price type used as the second part of technical indicator | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesSub200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesSum**
> GetTimeSeriesSum200Response getTimeSeriesSum()

The Summation (SUM) endpoint calculates the cumulative total of a specified data series over a defined time period. It returns a numerical value representing the sum, which can be used to track the aggregate value of financial data, such as stock prices or trading volumes, over time. This endpoint is useful for users needing to compute the total accumulation of a dataset for further analysis or reporting.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesSum(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesSum200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesSuperTrend**
> GetTimeSeriesSuperTrend200Response getTimeSeriesSuperTrend()

The Supertrend endpoint provides data on the Supertrend indicator, a tool used to identify potential buy and sell signals in trending markets. It returns values that indicate the current trend direction and potential reversal points based on price, time, and volatility. Users can leverage this data to pinpoint optimal entry and exit points for trades.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let period: number; //The period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 10)
let multiplier: number; //The factor used to adjust the indicator\'s sensitivity. (optional) (default to 3)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesSuperTrend(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    period,
    multiplier,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **period** | [**number**] | The period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 10|
| **multiplier** | [**number**] | The factor used to adjust the indicator\&#39;s sensitivity. | (optional) defaults to 3|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesSuperTrend200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesSuperTrendHeikinAshiCandles**
> GetTimeSeriesSuperTrendHeikinAshiCandles200Response getTimeSeriesSuperTrendHeikinAshiCandles()

The Supertrend Heikin Ashi candles endpoint provides data combining Supertrend signals with Heikin Ashi candlestick patterns. It returns a series of data points indicating trend direction and smoothed price movements, useful for identifying potential buy or sell opportunities in trading.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let period: number; //The period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 10)
let multiplier: number; //The factor used to adjust the indicator\'s sensitivity. (optional) (default to 3)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesSuperTrendHeikinAshiCandles(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    period,
    multiplier,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **period** | [**number**] | The period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 10|
| **multiplier** | [**number**] | The factor used to adjust the indicator\&#39;s sensitivity. | (optional) defaults to 3|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesSuperTrendHeikinAshiCandles200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesT3ma**
> GetTimeSeriesT3ma200Response getTimeSeriesT3ma()

The Triple Exponential Moving Average (T3MA) endpoint calculates a smoothed moving average using three exponential moving averages on price data. It returns a dataset that highlights price trends with reduced lag, offering precise trend analysis. This is useful for identifying trend direction and potential reversal points.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let vFactor: number; //The factor used to adjust the indicator\'s volatility. Takes values in the range from `0` to `1` (optional) (default to 0.7)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesT3ma(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    vFactor,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **vFactor** | [**number**] | The factor used to adjust the indicator\&#39;s volatility. Takes values in the range from &#x60;0&#x60; to &#x60;1&#x60; | (optional) defaults to 0.7|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesT3ma200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesTRange**
> GetTimeSeriesTRange200Response getTimeSeriesTRange()

The True Range (TRANGE) endpoint calculates the range of price movement for a specified period, providing a measure of market volatility. It returns data that includes the highest and lowest prices over the period, along with the closing price from the previous period. This information is useful for traders to assess market volatility and adjust their trading strategies accordingly.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesTRange(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesTRange200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesTema**
> GetTimeSeriesTema200Response getTimeSeriesTema()

The Triple Exponential Moving Average (TEMA) endpoint calculates and returns the TEMA values for a specified financial instrument over a given time period. This endpoint provides a series of data points that smooth out price fluctuations by applying three layers of exponential moving averages, allowing users to identify and track underlying trends in the instrument\'s price movement.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //The time period used for calculation in the indicator. Default is 9. (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesTema(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | The time period used for calculation in the indicator. Default is 9. | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesTema200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesTrima**
> GetTimeSeriesTrima200Response getTimeSeriesTrima()

The Triangular Moving Average (TRIMA) endpoint calculates and returns the smoothed average price of a financial security over a specified period, with a focus on central data points. This endpoint provides a balanced view of price trends by applying a double smoothing process, making it useful for identifying underlying price patterns and reducing short-term fluctuations.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesTrima(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesTrima200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesTsf**
> GetTimeSeriesTsf200Response getTimeSeriesTsf()

The Time Series Forecast (TSF) endpoint provides projected future price levels using linear regression analysis. It returns data that helps users identify potential support and resistance levels, as well as trend direction in a financial market. This endpoint is useful for traders seeking to anticipate price movements and adjust their strategies accordingly.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesTsf(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesTsf200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesTypPrice**
> GetTimeSeriesTypPrice200Response getTimeSeriesTypPrice()

The Typical Price (TYPPRICE) endpoint calculates and returns the average of a financial instrument\'s high, low, and close prices for a given period. This endpoint provides a simplified metric that reflects the central tendency of price movements, useful for traders and analysts who need a straightforward view of price trends.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesTypPrice(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesTypPrice200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesUltOsc**
> GetTimeSeriesUltOsc200Response getTimeSeriesUltOsc()

The Ultimate Oscillator endpoint (/ultosc) calculates a momentum oscillator that integrates short, intermediate, and long-term price movements to detect potential overbought or oversold conditions and possible trend reversals. It returns a time series of oscillator values, which can be used to assess market momentum and identify entry or exit points in trading strategies.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod1: number; //The first time period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 7)
let timePeriod2: number; //The second time period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 14)
let timePeriod3: number; //The third time period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 28)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesUltOsc(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod1,
    timePeriod2,
    timePeriod3,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod1** | [**number**] | The first time period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 7|
| **timePeriod2** | [**number**] | The second time period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **timePeriod3** | [**number**] | The third time period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 28|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesUltOsc200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesVar**
> GetTimeSeriesVar200Response getTimeSeriesVar()

The Variance (VAR) endpoint calculates the statistical variance of a financial data series, providing a measure of how much the data points deviate from the average value. It returns a numerical value representing this dispersion, which can be used to assess the volatility of a security over a specified period. This information is crucial for traders and analysts who need to evaluate the risk associated with price fluctuations in the market.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesVar(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesVar200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesVwap**
> GetTimeSeriesVwap200Response getTimeSeriesVwap()

The Volume Weighted Average Price (VWAP) endpoint provides the VWAP value for a specified stock or asset over a given time period. This indicator calculates the average price at which a security has traded throughout the day, based on both volume and price. It is useful for identifying the true average price of an asset, helping traders to assess the current price relative to the day\'s average.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let sdTimePeriod: number; //The time period for the standard deviation calculation. Must be greater than `0`. Recommended value is `9`. This parameter is only used together with `sd`. (optional) (default to 0)
let sd: number; //The standard deviation applied in the calculation. Must be greater than `0`. Recommended value is `2`. This parameter is only used together with `sd_time_period`. (optional) (default to 0)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesVwap(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    sdTimePeriod,
    sd,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **sdTimePeriod** | [**number**] | The time period for the standard deviation calculation. Must be greater than &#x60;0&#x60;. Recommended value is &#x60;9&#x60;. This parameter is only used together with &#x60;sd&#x60;. | (optional) defaults to 0|
| **sd** | [**number**] | The standard deviation applied in the calculation. Must be greater than &#x60;0&#x60;. Recommended value is &#x60;2&#x60;. This parameter is only used together with &#x60;sd_time_period&#x60;. | (optional) defaults to 0|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesVwap200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesWclPrice**
> GetTimeSeriesWclPrice200Response getTimeSeriesWclPrice()

The Weighted Close Price (WCLPRICE) endpoint calculates a security\'s average price by giving additional weight to the closing price, using the formula: (High + Low + Close * 2) / 4.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesWclPrice(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesWclPrice200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesWillR**
> GetTimeSeriesWillR200Response getTimeSeriesWillR()

The Williams %R (WILLR) endpoint calculates the Williams Percent Range, a momentum indicator that evaluates a security\'s closing price relative to its high-low range over a specified period. This endpoint returns data that helps users identify potential overbought or oversold conditions and possible trend reversals in the market.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesWillR(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 14|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesWillR200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

# **getTimeSeriesWma**
> GetTimeSeriesWma200Response getTimeSeriesWma()

The Weighted Moving Average (WMA) endpoint calculates and returns the WMA values for a given security over a specified period. This endpoint provides a time series of weighted averages, where recent prices have a higher influence, allowing users to track and analyze short-term price trends effectively.

### Example

```typescript
import {
    TechnicalIndicatorApi,
    Configuration
} from '@twelvedata/twelvedata-node';

const configuration = new Configuration();
const apiInstance = new TechnicalIndicatorApi(configuration);

let interval: IntervalEnum; //Interval between two consecutive points in time series (default to undefined)
let symbol: string; //Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional) (default to undefined)
let isin: string; //Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let figi: string; //The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional) (default to undefined)
let cusip: string; //The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional) (default to undefined)
let outputsize: number; //Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
let exchange: string; //Exchange where instrument is traded (optional) (default to undefined)
let micCode: string; //Market Identifier Code (MIC) under ISO 10383 standard (optional) (default to undefined)
let country: string; //The country where the instrument is traded, e.g., `United States` or `US` (optional) (default to undefined)
let type: TypeEnum; //The asset class to which the instrument belongs (optional) (default to undefined)
let timezone: string; //Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to 'Exchange')
let startDate: string; //Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional) (default to undefined)
let endDate: string; //The ending date and time for data selection, see `start_date` description for details. (optional) (default to undefined)
let date: string; //Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional) (default to undefined)
let order: OrderEnum; //Sorting order of the output (optional) (default to undefined)
let prepost: boolean; //Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
let format: FormatEnum; //The format of the response data (optional) (default to undefined)
let delimiter: string; //The separator used in the CSV response data (optional) (default to ';')
let dp: number; //Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
let previousClose: boolean; //A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
let adjust: AdjustEnum; //Adjusting mode for prices (optional) (default to undefined)
let seriesType: SeriesTypeEnum; //Price type on which technical indicator is calculated (optional) (default to undefined)
let timePeriod: number; //Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
let includeOhlc: boolean; //Specify if OHLC values should be added in the output (optional) (default to false)

const { status, data } = await apiInstance.getTimeSeriesWma(
    interval,
    symbol,
    isin,
    figi,
    cusip,
    outputsize,
    exchange,
    micCode,
    country,
    type,
    timezone,
    startDate,
    endDate,
    date,
    order,
    prepost,
    format,
    delimiter,
    dp,
    previousClose,
    adjust,
    seriesType,
    timePeriod,
    includeOhlc
);
```

### Parameters

|Name | Type | Description  | Notes|
|------------- | ------------- | ------------- | -------------|
| **interval** | **IntervalEnum** | Interval between two consecutive points in time series | defaults to undefined|
| **symbol** | [**string**] | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | (optional) defaults to undefined|
| **isin** | [**string**] | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **figi** | [**string**] | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | (optional) defaults to undefined|
| **cusip** | [**string**] | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | (optional) defaults to undefined|
| **outputsize** | [**number**] | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | (optional) defaults to 30|
| **exchange** | [**string**] | Exchange where instrument is traded | (optional) defaults to undefined|
| **micCode** | [**string**] | Market Identifier Code (MIC) under ISO 10383 standard | (optional) defaults to undefined|
| **country** | [**string**] | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | (optional) defaults to undefined|
| **type** | **TypeEnum** | The asset class to which the instrument belongs | (optional) defaults to undefined|
| **timezone** | [**string**] | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | (optional) defaults to 'Exchange'|
| **startDate** | [**string**] | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | (optional) defaults to undefined|
| **endDate** | [**string**] | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | (optional) defaults to undefined|
| **date** | [**string**] | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | (optional) defaults to undefined|
| **order** | **OrderEnum** | Sorting order of the output | (optional) defaults to undefined|
| **prepost** | [**boolean**] | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | (optional) defaults to false|
| **format** | **FormatEnum** | The format of the response data | (optional) defaults to undefined|
| **delimiter** | [**string**] | The separator used in the CSV response data | (optional) defaults to ';'|
| **dp** | [**number**] | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | (optional) defaults to -1|
| **previousClose** | [**boolean**] | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | (optional) defaults to false|
| **adjust** | **AdjustEnum** | Adjusting mode for prices | (optional) defaults to undefined|
| **seriesType** | **SeriesTypeEnum** | Price type on which technical indicator is calculated | (optional) defaults to undefined|
| **timePeriod** | [**number**] | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | (optional) defaults to 9|
| **includeOhlc** | [**boolean**] | Specify if OHLC values should be added in the output | (optional) defaults to false|


### Return type

**GetTimeSeriesWma200Response**

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

 - **Content-Type**: Not defined
 - **Accept**: application/json


### HTTP response details
| Status code | Description | Response headers |
|-------------|-------------|------------------|
|**200** |  |  -  |

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to Model list]](../README.md#documentation-for-models) [[Back to README]](../README.md)

